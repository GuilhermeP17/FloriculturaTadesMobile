<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.senac.projectsolutions">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/floricultura_logo_launcher"
        android:supportsRtl="true"
        android:theme="@style/LoginTheme">

        <activity android:name=".View.LoginActivity"/>

        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".View.CadastroActivity" />
        <activity android:name=".View.CarrinhoActivity" />
        <activity android:name=".View.DescricaoActivity" />
        <activity android:name=".View.EnderecoCarrinhoActivity" />
        <activity android:name=".View.PerfilActivity" />
        <activity android:name=".View.PedidosFinalizadosActivity" />
        <activity android:name=".View.PedidosAndamentoActivity" />
    </application>
</manifest>
